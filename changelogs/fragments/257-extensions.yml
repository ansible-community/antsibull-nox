bugfixes:
  - "When removing files from a set of matches, for example for applying a separate linter configuration for modules, file extensions were handled incorrectly
     (https://github.com/ansible-community/antsibull-nox/pull/257)."
